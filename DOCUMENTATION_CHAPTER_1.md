# مستند التوثيق الفني والأكاديمي الرسمي للنظام
## الفصل الأول: المدخل العام والإطار المفهومي للمشروع (Introduction & Project Framework)

---

### 1.1 المقدمة والمدخل العام (General Introduction)

تعد حماية البيانات والخصوصية من الركائز الأساسية للحوكمة الرقمية والأمن السيبراني في المؤسسات الحديثة. يتطلب الامتثال لممارسات الخصوصية إطار عمل منهجيًا يجمع بين المعرفة المعيارية، الأدوات التقنية، وإجراءات التدقيق المحسّنة بالأتمتة والذكاء الاصطناعي.

يقدّم هذا المشروع "نظام تقييم ومراجعة حماية البيانات والخصوصية" منصة متكاملة تساعد مدقّقي الأمن والخصوصية والمختصين على:

- استيراد وفحص سياسات الخصوصية (ملفات PDF/CSV/نصوص أو عن طريق زحف المواقع)،
- مطابقة نصوص السياسات مع ضوابط معيارية (NIST SP 800-53 Rev 5)،
- إجراء تقييمات يدوية ومرشّحات آلية مقترحة بواسطة الذكاء الاصطناعي (Gemini عبر @google/genai)،
- إنشاء تقارير تفاعلية وتصديرها (PDF، طباعة)،
- إدارة مستخدمين، سجلات النظام، ومجموعة بيانات الضوابط.

الواجهة الأمامية مبنية بـ React + TypeScript وتعمل على Vite، بينما يحتوي جانب الخادم على خادم Express بسيط للتكامل مع واجهات الذكاء الاصطناعي ومعالجة الطلبات.

---

### 1.2 مشكلة المشروع (Problem Statement)

تواجه المؤسسات والمدققون مجموعة من المشاكل عند محاولة قياس حالة الامتثال لحماية البيانات، من أبرزها:

1. التعقيد والوقت الطويل اللازم للتدقيق اليدوي لوثائق السياسات وخرائط الضوابط.
2. صعوبة الربط الدقيق بين نصوص السياسات (التي تكون غير موحّدة الصياغة) وبين أحكام ومعايير محددة في كتالوج الضوابط.
3. نقص الأدوات المدمجة التي تجمع بين استخراج النصوص من مستندات/مواقع والقدرة على اقتراح تقييمات مدفوعة بالذكاء الاصطناعي.
4. تشتت البيانات والحاجة إلى نافذة موحّدة لإدارة الضوابط والتقارير ونتائج التدقيق.

هذا المشروع يعالج هذه النقاط بتجميع سلسلة أدوات متكاملة: مستودع ضوابط معياري، محرّك استيراد/زحف للسياسات، محرك تقييم بنهج هجين (يدوي + AI)، ولوحة تقارير تفاعلية.

---

### 1.3 أهداف المشروع (Project Objectives)

الأهداف التقنية والوظيفية للمشروع هي:

- أتمتة عملية تقييم الامتثال عبر مساعدة ذكية لاستخراج البنود المطابقة من الوثائق والمواقع.
- اعتماد إطار NIST SP 800-53 Rev 5 كمرجع رئيسي للضوابط وتقديم تمثيل ثنائي اللغة (العربية/الإنجليزية).
- توفير واجهة مستخدم تتيح:
  - إدارة الضوابط وقواعد البيانات المرتبطة بها.
  - تقييم كل ضبط (نعم/جزئي/لا/غير مطبق/غير مُجاب) مع تدوين الأدلة والملاحظات.
  - تجميع التقارير والرسوم البيانية لتوضيح مستوى الامتثال.
- دعم رفع الملفات (PDF/CSV) وزحف المواقع لاستخراج نصوص السياسات وملاءمتها مع الضوابط.
- تمكين أدوار المستخدمين (RBAC): مدير نظام (Admin)، مدقق (User)، مع سجلات نشاط مفصلة.

---

### 1.4 نطاق العمل وحدود النظام (Scope of Work & Boundaries)

#### أولاً: النطاق الوظيفي (Functional Scope)

1. إدارة الحسابات والمصادقة (محليًا في الواجهة مع تخزين مبسّط في localStorage).
2. استيراد ملفات السياسات (PDF/CSV) واستخراج النصوص.
3. أداة زحف ويب لاكتشاف صفحات سياسات الخصوصية واستخلاص النصوص الأساسية.
4. محرّك تقييم الضوابط مع إمكانية التحرير اليدوي وتطبيق اقتراحات AI.
5. لوحة تقارير تفاعلية (مخططات، ملخصات، وتصدير PDF).
6. واجهة إدارة Dataset وAdmin Dashboard لعمليات الصيانة.

#### ثانياً: النطاق التقني (Technical Scope)

- Frontend: React (v19) + TypeScript + Vite + Tailwind CSS.
- Backend: Node.js + Express (server.ts) — يعالج طلبات التكامل مع GPT/Gemini عبر حزمة @google/genai.
- أدوات مساعدة: jsPDF و html2canvas لتوليد التقارير، pdf-parse لمعالجة PDF.
- التخزين المحلي للتطبيق: استخدام localStorage لبيانات التطوير والتجريب.

#### ثالثاً: الحدود والاستثناءات (Out of Scope)

- لا يقوم النظام بتغيير أو تحرير خوادم الجهات الخارجية أو محتوى مواقع غير مملوكة.
- لا يوفّر نظام إدارة هويات مركزي (SSO/LDAP) بشكل افتراضي — يمكن ربطه لاحقًا.
- النسخ الاحتياطي والنسخ الآمن طويل المدى للبيانات ليست مغطاة افتراضيًا (يتوجب تضمين قاعدة بيانات خارجية عند نشر إنتاجي).

---

### 1.5 الأهمية والقيمة المضافة للمشروع (Project Significance & Strategic Value)

- تقليل الجهد الزمني لعمليات المراجعة والتقييم بنسبة كبيرة عبر اقتراحات الذكاء الاصطناعي واستخلاص النصوص الآلي.
- زيادة دقّة المطابقة بين نصوص السياسات والضوابط القياسية عبر محركات البحث النصي ومطابقة المقاطع.
- توفير بيانات قابلة للقياس للإدارة (KPIs) مثل نسبة الامتثال العام، وضع العائلات، والمخاطر المفتوحة.
- تمكين فرق التدقيق من إنتاج تقارير رسمية جاهزة للطباعة وتصديرها بصيغة PDF.

---

### 1.6 المنهجية والمعايير المعتمدة (Methodology & Standards)

1. اعتماد NIST SP 800-53 Rev 5 كمصدر رئيسي للضوابط: تُخزّن الضوابط داخل المشروع (انظر src/data/nist_catalog) وتُعرض بلغة ثنائية.
2. منهجية تطوير Full-Stack: فصل طبقة العرض (React) عن طبقة المعالجة (Express + خدمات AI) مع تركيز على واجهة برمجة تطبيقات بسيطة.
3. معالجة آمنة للسرية: مفتاح GEMINI_API_KEY يجب أن يبقى في بيئة الخادم (.env.local) وعدم تضمينه في الواجهة.
4. تعامل هجيني مع الذكاء الاصطناعي: AI لتقديم اقتراحات ومطابقة نصوص/استنتاجات، بينما القرار النهائي للتقييم يبقى للمراجع البشري.

---

### 1.7 هيكلية وثيقة التوثيق (Document Structure Overview)

سلسلة التوثيق ستتضمن الفصول التالية (موجزة):

- الفصل الأول: المدخل العام والإطار المفهومي للمشروع — (هذا الملف)
- الفصل الثاني: الدراسة المرجعية وتحليل المتطلبات — متطلبات وظيفية وغير وظيفية، حالات الاستخدام، مخططات المستخدم
- الفصل الثالث: التصميم المعماري وهيكلية النظام — مخططات مكونات، تدفق الطلبات، واجهات API، نموذج البيانات
- الفصل الرابع: التنفيذ البرمجي والمكونات الأساسية — وصف ملفات المصدر، خيوط التنفيذ المهمة، أمثلة على المسارات والواجهات
- الفصل الخامس: الاختبارات، التقييم، والتقارير — خطة اختبار، أمثلة بيانات، معايير القبول
- الفصل السادس: النشر، الصيانة، والتوصيات المستقبلية — سيناريوهات النشر، ترقية المكونات، اقتراحات للتطوير

---

### 1.8 مراجع داخل المشروع (In-Repo References)

بعض الملفات والمواقع الأساسية داخل المستودع التي يستند إليها هذا الفصل:

- README.md — إرشادات التشغيل الأساسية وذكر GEMINI_API_KEY.
- package.json — سكربتات التشغيل (npm run dev / build / start) والاعتماديات.
- server.ts — مدخل خادم Express للتكامل مع خدمات الذكاء الاصطناعي ونشر الواجهة.
- src/App.tsx — التطبيق الرئيسي وإدارة الحالة والتبويب.
- src/types.ts — تعريفات الأنواع (Control, Evaluation, User, CompanyProfile).
- src/data/ (NIST catalog) — ملف NIST_SP800_53_REV5_CONTROLS ومصادر الضوابط.
- src/utils/aiTranslatorService.ts — خدمة التكامل مع Gemini/GenAI (مهمّ لآليات الترجمة والاقتراحات الآلية).
- src/components/* — تبويبات الواجهة: EvaluationTab, PolicyQuestionnaireTab, WebCrawlerTab, DashboardReportTab, DatasetStudioTab, AdminPanelTab

---

### 1.9 تشغيل المشروع (مختصر) — ملاحظة تشغيلية مقتضبة

1. استنساخ المشروع وتشغيل الاعتماديات:

```bash
git clone https://github.com/ibrahammohadahamada/Evaluation-System.git
cd Evaluation-System
npm install
```

2. إعداد متغير البيئة:
- إنشاء ملف .env.local أو ضبط البيئة بحيث يحتوي على:
  - GEMINI_API_KEY="<مفتاحك>"

3. تشغيل بيئة التطوير:

```bash
npm run dev
# هذا يشغّل server.ts عبر tsx ويبدأ خادم Vite لواجهة React
```

4. بناء للإنتاج وتشغيل:

```bash
npm run build
npm start
```

---

تم إعداد هذا الفصل الأول بصيغة نهائية قابلة للحفظ داخل ملف DOCUMENTATION_CHAPTER_1.md. إذا رغبت، أستطيع الآن:

- حفظ هذا المحتوى إلى المستودع (تحديث الملف الموجود)،
- أو تقسيم الفصل إلى ملفات فرعية (مثلاً docs/chapter-1/overview.md وdocs/chapter-1/run.md)،
- أو المتابعة فورًا بكتابة الفصل الثاني (تحليل المتطلبات) بناءً على ملفات الكود ومكونات المشروع.
