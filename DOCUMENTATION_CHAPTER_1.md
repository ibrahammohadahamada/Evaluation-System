# مستند التوثيق الفني والأكاديمي الرسمي للنظام
## الفصل الأول: المدخل العام والإطار المفهومي للمشروع (Introduction & Project Framework)

---

### 1.1 المقدمة والمدخل العام (General Introduction)
تعد حماية البيانات والخصوصية من الركائز الأساسية للحوكمة الرقمية والأمن السيبراني في المؤسسات الحديثة. يتطلب الامتثال لممارسات الخصوصية إطارًا منهجيًا يجمع بين المرجعيات القياسية، أدوات الاستخلاص والتحليل، وإجراءات التقييم التي تدعم اتخاذ القرار البشري.

### 1.2 مشكلة المشروع (Problem Statement)
تواجه المؤسسات والمدققون صعوبات في مطابقة نصوص السياسات مع ضوابط معيارية، استغراق وقت طويل في التدقيق اليدوي، وتشتت الأدوات المستخدمة. يسعى هذا المشروع لتقليل هذه العقبات عبر منصة متكاملة تجمع الزحف، الاستخلاص، والتحليل المدعوم بالذكاء الاصطناعي.

### 1.3 أهداف المشروع (Project Objectives)
- تقديم منصة شبه مؤتمتة لتقييم الامتثال للخصوصية
- تمكين مطابقة نصية دقيقة بين سياسات المؤسسات ومصطلحات ضوابط NIST
- توفير تقارير قابلة للطباعة والتحليل وإدارة سجلات النشاط
- دعم الأدوار والتفويض (RBAC) مع واجهة استخدام ثنائية اللغة (عربي/إنجليزي)

### 1.4 نطاق العمل وحدود النظام (Scope of Work & Boundaries)
يتضمن النطاق الوظيفي والتقني وحدود الاستثناءات؛ من بينها إدارة المستخدمين، استيراد/زحف السياسات، محرّك التقييم، وDashboard للتقارير. لا يشمل التحكم بالخوادم الخارجية أو SSO الافتراضي.

### 1.5 الأهمية والقيمة المضافة للمشروع (Project Significance)
يوفر المشروع تسريعًا في عمليات المراجعة، دقة أعلى في المطابقة، وبيانات قابلة للقياس (KPIs) تهم الإدارات العليا ومسؤولي الامتثال.

### 1.6 المنهجية والمعايير المعتمدة (Methodology & Standards)
يعتمد على NIST SP 800-53 Rev 5، نهج Full-Stack، ومبدأ المعالجة الآمنة لمفاتيح API (تشغيل التكامل مع Gemini في الخادم فقط).

### 1.7 هيكلية الوثيقة (Document Structure Overview)
تفصيل الفصول: 1 (مدخل) — 6 (خاتمة وتوصيات). يُوضِّح كل فصل عناصره ونطاقه.

### 1.8 المراجع داخل المستودع (In-Repo References)
قائمة الملفات الأساسية: README.md, package.json, server.ts, src/App.tsx, src/types.ts, src/data/, src/utils/...

### 1.9 كيفية التشغيل (Quick Start)
أوامر: git clone ...; npm install; إعداد GEMINI_API_KEY في .env.local; npm run dev؛ build/start للبيئة الإنتاجية.

### 1.10 تعريف المصطلحات والاختصارات (Definitions & Abbreviations)
- NIST: National Institute of Standards and Technology
- RBAC: Role-Based Access Control
- AI/Gemini: محرك الذكاء الاصطناعي المستخدم عبر @google/genai
- PDF/CSV: صيغ الملفات المدعومة للاستخراج

### 1.11 أصحاب المصلحة (Stakeholders)
- فريق الامتثال والمخاطر
- مدققون داخليون وخارجيون
- قسم تقنية المعلومات (IT)
- الإدارة العليا (CISO/DPO)

### 1.12 المتطلبات العامة (High-level Requirements)
- واجهة مستخدم تتيح تقييم الضوابط وإدارة الملفات
- وحدة زحف ومستخلص نصوص للويب وملفات PDF/CSV
- تكامل آمن مع خدمات AI عبر خادم وسيط

### 1.13 المتطلبات غير الوظيفية (Non-functional Requirements)
- أداء واجهة تفاعلية لصفحات الضوابط
- حماية أسرار (مفاتيح API) وعدم تسريبها للعميل
- قابلية التوسع عند الانتقال إلى بيئة إنتاج
- قابلية التدويل (RTL/LTR ودعم عربي/إنجليزي)

### 1.14 افتراضات المشروع (Assumptions)
- يستخدم النظام بيانات تجريبية مخزنة محليًا (localStorage) في وضع التطوير
- تعتمد نتائج اقتراحات AI على جودة النصوص المدخلة وقيود نموذج Gemini
- النشر الإنتاجي سيتطلب قاعدة بيانات وإدارة أسرار خارجية

### 1.15 قيود النظام (Constraints)
- بطبيعة الحال، تحليل AI يعتمد على قدرة الخدمة الخارجية ومعدلات الاستخدام
- محدودية التخزين المحلي لا تناسب سيناريوهات بيانات كبيرة

### 1.16 نموذج البيانات (Data Model Overview)
وصف مختصر للكيانات الرئيسة (NistControl, ControlEvaluation, UploadedFileRecord, CompanyProfile, UserAccount) موجودة في src/types.ts.

### 1.17 أمن وخصوصية البيانات (Security & Privacy Considerations)
- عدم إدراج مفاتيح API في العميل.
- حفظ أقل قدر ممكن من البيانات الحساسة في localStorage عند التطوير.
- تشفير القنوات عبر HTTPS عند النشر.

### 1.18 سياسات الاحتفاظ بالبيانات (Data Retention)
اقتراح سياسة افتراضية: احتفاظ بيانات التشغيل المحلية خلال 90 يومًا، وإمكانية تصدير/حذف البيانات يدويًا عبر واجهة الإدارة.

### 1.19 سجلات النظام والمراجعة (Logging & Audit Trails)
يتم تسجيل أحداث الدخول والتعديلات والتقييمات في سجلات النظام (logs) داخل الواجهة؛ يُنصح بربطها بنظام مركزي عند النشر.

### 1.20 الأداء وقابلية التوسع (Performance & Scalability)
في وضع الإنتاج يُنصح بفصل خدمات الزحف والتحليل إلى وظائف خلفية (workers)، واستخدام قاعدة بيانات معيارية (Postgres/Mongo) وكاش (Redis) للنتائج المتكررة.

### 1.21 التدويل والوصول متعدد اللغات (Localization)
التطبيق يدعم العربية والإنجليزية؛ يجب متابعة ترجمة النصوص الثابتة واختبار اتجاه المستند (RTL) للواجهات.

### 1.22 المخاطر والقيود التشغيلية (Risks & Operational Limitations)
- تبعية على API خارجي (تكلفة/توافر)
- جودة اقتراحات AI قد تؤدي إلى نتائج خاطئة إن لم يتم مراجعتها
- ملفات PDF مع تنسيقات غير قياسية قد تفشل عملية الاستخلاص

### 1.23 المتابعة والتوصيات (Next Steps & Recommendations)
- كتابة الفصل الثاني: تحليل المتطلبات التفصيلي وحالات الاستخدام
- إعداد بيئة اختبار متكاملة مع مفتاح Gemini اختباري
- تصميم نموذج نشر إنتاجي (قاعدة بيانات، إدارة أسرار، CI/CD)
- إضافة اختبارات وحدة وتكامل للأجزاء الحرجة (parser, ai service, evaluation engine)

---

تم تحديث الفصل الأول ليشمل الأقسام 1.1 وحتى 1.23 مفصّلة بصيغة فنية عملية. إذا رغبت أستطيع الآن:
- حفظ التغييرات للمستودع (سأقوم بالتعديل مباشرة في DOCUMENTATION_CHAPTER_1.md)،
- أو تقسيم كل قسم إلى ملفات منفصلة داخل docs/chapter-1/،
- أو بدء كتابة الفصل الثاني بتفصيل متطلبات الاستخدام ونماذج البيانات.
